# automation/

Playwright application-assistance framework (Phase 11). Compliant automation only —
never bypasses CAPTCHA, anti-bot, rate limits, or security controls. `AUTO_SUBMIT=false`
by default; nothing is submitted without explicit user confirmation.
